services:
  metro:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8081:8081"
    volumes:
      - ./src:/app/src
      - ./App.tsx:/app/App.tsx
    command: npm start -- --host 0.0.0.0
